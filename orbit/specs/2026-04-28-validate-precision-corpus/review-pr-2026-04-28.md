# Pre-Merge Review

**Date:** 2026-04-28
**Reviewer:** Context-separated agent (fresh session)
**Branch:** spec/validate-precision-corpus
**Spec:** /Users/hugh/github/meridian-online/finetype/orbit/specs/2026-04-28-validate-precision-corpus/spec.yaml (v1.1)
**Verdict:** APPROVE

---

## Test Results

| Check | Result | Details |
|-------|--------|---------|
| `cargo test --workspace` | PASS | 785 passed, 33 ignored, 0 failed (45 suites, 13.81s) |
| `make ci` (fmt + clippy + test + check) | PASS | exit 0 |
| `cargo clippy --workspace --all-targets -- -D warnings` | PRE-EXISTING FAIL | 3 errors on this branch; **same 3 errors on `main`** before this branch existed (verified via `git stash`). Not introduced by this PR. |
| `pvc_*` test suite | PASS | 10/10 (6 attribute_*, 1 widening, 3 enum_emission) |
| `python3 scripts/eval_leakage/test_validate_corpus_firewall.py` | PASS | 3/3 (dry-run reads both manifests; --no-validate skips; full grows hash set 237 860→288 700) |
| Harness binary build | PASS | `cargo build --release -p finetype-eval --bin validate-corpus` clean |
| Round-trip harness output | PRESENT | `eval/eval_output/validate_corpus.md` and `.baseline.md` both committed; delta line present (`delta: +0`); all 6 mechanism rows in breakdown table |

## AC Coverage Report

Test prefix: `pvc` (per `metadata.test_prefix`).

| AC | Type | Status | Test(s) / Evidence |
|----|------|--------|--------------------|
| ac-01 | code | PASS | `validate_manifest.csv` 9 cols × 7 rows; no `unknown` source_url; every `file_path`/`gt_sidecar_path` resolves on disk |
| ac-02 | code | PASS | sources.yaml contains 7 `role: validate` entries with all 6 required keys; existing data rows untouched (only header-comment block rewrites + appended block, which the spec itself mandates) |
| ac-03 | code | PASS | `compute_row_hashes.py` reads both manifests via DictReader; dry-run reports `manifest [eval]` and `manifest [validate]` lines; `scripts/eval_leakage/test_validate_corpus_firewall.py` ALL PASS |
| ac-04 | code | PASS | All 7 sidecars present; 58 columns total; every CSV header column appears in the corresponding sidecar |
| ac-05 | code | PASS | `crates/finetype-eval/src/bin/validate_corpus.rs` (~767 LOC); cargo build clean; binary runs |
| ac-06 | code | PASS | `validate-corpus` Makefile target with `cargo build --release --bin finetype` and `--bin validate-corpus` prereqs; `eval-report` invokes it and appends headline |
| ac-07 | code | PASS | 6 deterministic rules + 6 `pvc_attribute_*` unit tests adjacent to binary; all green |
| ac-08 | code | PASS | Report shape matches: headline + `(baseline:; delta:)` + per-mechanism (all 6 mechanisms literally present) + per-dataset + per-column tables |
| ac-09 | code | PASS | `--enum-threshold` default `"32"` (line 301); shared lib at `crates/finetype-cli/src/enum_emission.rs` matches `representation.discrete.categorical` + 3 `representation.boolean.*` labels; T1/T2/T3 tests pass |
| ac-10 | code | PASS | One widening (`representation.numeric.decimal_number` regex gains optional `[eE][+-]?[0-9]+`); 1 widening within ≤5 bound; `pvc_widening_decimal_number_accepts_scientific_notation` passes (16-value accept, 14-value reject) |
| ac-11 | doc | PASS | MADRs 0072, 0073, 0074 land; status `accepted`, date `2026-04-28`; ≥3 considered options each |
| ac-12 | doc | PASS | CHANGELOG `[Unreleased]` has `validate-corpus`, `enum-threshold`, `decimal_number` widening text; CLAUDE.md "What's next" cites `validate-precision-corpus` spec |
| ac-13 | gate | PASS (with caveat) | `make ci` exits 0; the spec's secondary verification `cargo clippy --workspace --all-targets -- -D warnings` fails on **both** branch and main (3 pre-existing errors in `finetype-candle-spike`, `finetype-eval/eval_extract.rs`, `finetype-duckdb`) — not regressed by this PR |
| ac-14 | gate | PASS | Both `validate_corpus.md` and `validate_corpus.baseline.md` committed; `grep "delta:"` matches |
| ac-15 | doc | PASS | `orbit/cards/0015-validate-corpus-curation.yaml` exists; `maturity: emerging`; 3 scenarios; references the spec |

**Coverage:** 12/12 testable (`code`) ACs have tests. 3 ACs are exempt (`doc`, `doc`, `doc`/`gate`). Ten `pvc_*` tests cover ac-07/09/10 directly; ac-05 covered by binary build + cargo build; ac-03 by Python regression test.

## Findings

### [LOW] Pre-existing clippy errors not addressed
**Category:** environment-mismatch
**Description:** ac-13's verification clause includes `cargo clippy --workspace --all-targets -- -D warnings exits 0`, but this command fails with 3 errors on **main** as well as on the branch — they pre-date this PR (in `finetype-candle-spike/tests/candle_validation.rs`, `finetype-eval/src/bin/eval_extract.rs`, `finetype-duckdb/src/lib.rs`).
**Evidence:**
```
git stash; cargo clippy --workspace --all-targets -- -D warnings; echo $?
# main_rc=101  (3 errors, all pre-existing)
git stash pop
```
The PR's own progress.md flagged this as "awaiting workspace clippy --all-targets confirmation"; `make ci` (the canonical gate) passes cleanly.
**Recommendation:** Spec verification clause is over-strict given the existing repo state — either accept `make ci` as the gate (consistent with existing CI) or open a hygiene card for the 3 pre-existing warnings. No action required for this PR.

### [INFO] sources.yaml header-comment block edited (not "byte-unchanged")
**Category:** spec-text-vs-implementation
**Description:** ac-02 reads "Existing role-`eval` and role-`train` and role-`both-forbidden` entries are byte-unchanged." The data **entries** under `sources:` are indeed byte-unchanged (verified by inspecting the diff — additions only). However the file's leading documentation comment block was rewritten to describe the new `validate` role. The spec also says (paragraph below): "the header comment block of sources.yaml documents the new value with the same paragraph shape as the existing three roles" — which **requires** comment edits. The two clauses point in slightly different directions; the implementer chose the consistent interpretation (data rows unchanged; doc comments updated to document the new role).
**Evidence:** `git --no-pager diff main -- eval/datasets/sources.yaml` shows comment-block rewrites at the file head plus additions at the end; `version:` and all `sources:` data rows are untouched.
**Recommendation:** No code change. The interpretation is reasonable. If desired, future spec wording can be tightened to "data rows byte-unchanged."

### [INFO] Honest delta reporting in progress.md
**Category:** documentation-quality
**Description:** Post-fix harness re-run shows headline unchanged (3 of 7 → 3 of 7, delta +0). The progress.md and headline both make clear this is mechanism-level movement (format_diversity 1→0; us_baby_names.percent now validates; enum_overfit unchanged because remaining failures are taxonomy-baked enums on misclassified labels, not runtime cardinality enums). This honesty matches MADR 0072's "movement-based success" framing and the spec's `evaluation_principles` weight.
**Evidence:** `eval/eval_output/validate_corpus.md` line 7 + 12-13 vs baseline.
**Recommendation:** None — this is correct framing.

---

## Honest Assessment

The PR ships exactly what the spec describes: a working profile→validate round-trip harness over 7 fresh public-domain CSVs, with deterministic mechanism attribution, full GT sidecars (58 columns), three MADRs, a stub follow-up card, and two genuine in-scope fixes. All 15 ACs are satisfied — 12 with code+tests, 3 with documentation deliverables. The test suite (785 tests) passes cleanly under `make ci`. The two fixes (ac-09 enum-threshold + boolean.* gate parity, ac-10 decimal_number scientific-notation widening) are honest about their effect — format_diversity drops from 1 to 0, dataset pass count is unchanged. The decimal_number widening is supported by an explicit accept/reject regression test that pins the precision principle (MADR 0001) bound. The harness binary, the leakage firewall extension, and the GT sidecar curation are all in good shape and grow naturally via PR-only manifest edits per the stub card 0015. The only friction point is the spec's clippy `--all-targets` verification clause, which fails identically on `main` (pre-existing baseline) — not a PR regression.
