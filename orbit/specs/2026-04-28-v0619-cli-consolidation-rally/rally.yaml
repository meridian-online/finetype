rally: "v0.6.19 CLI surface consolidation"
autonomy: auto
phase: complete
started: 2026-04-28T01:12:33Z
completed: 2026-04-28T13:00:00Z
cards:
  - path: orbit/cards/0003-tabular-data-profiling.yaml
    status: shipped
    spec_dir: orbit/specs/2026-04-28-profile-json-schema-output/
    branch: rally/profile-json-schema-output
    pr: https://github.com/meridian-online/finetype/pull/54
    worktree: main
    parked_constraint: null
    ship_note: |
      Card 0003 work folded into PR #54's combined squash alongside
      card 0006 (the "Schema verb fold" rally merge). PR #53 was closed
      without merge — its branch had diverged from main after the rally
      chose the combined-squash path. All card 0003 deliverables
      (profile -o json-schema, --stats flag, emit_table_schema, MCP
      ProfileFormat::JsonSchema branch, golden_profile_json_schema_*
      tests) verified present on main.
  - path: orbit/cards/0005-schema-driven-data-validation.yaml
    status: shipped
    spec_dir: orbit/specs/2026-04-28-validate-absorbs-load/
    branch: rally/validate-absorbs-load
    pr: https://github.com/meridian-online/finetype/pull/56
    worktree: null
    parked_constraint: null
  - path: orbit/cards/0006-command-line-interface.yaml
    status: shipped
    spec_dir: orbit/specs/2026-04-28-schema-verb-fold/
    branch: rally/schema-verb-fold
    pr: https://github.com/meridian-online/finetype/pull/54
    worktree: main
    parked_constraint: null
implementation_order:
  - orbit/cards/0003-tabular-data-profiling.yaml
  - orbit/cards/0006-command-line-interface.yaml
  - orbit/cards/0005-schema-driven-data-validation.yaml
order_rationale: |
  Serial 0003 → 0006 → 0005. Cards 0003 and 0006 share OutputFormat::JsonSchema
  enum variant and crates/finetype-cli/src/json_schema.rs helper module; 0003
  lands the helper and the variant first, then 0006 extends the helper for
  type-mode and deletes the schema verb. 0005 (validate absorbs load) is
  logically independent but ordered last to avoid main.rs merge conflicts and
  keep the PR stack linear.
notes: |
  Riding alongside the rally as separate specs (not card inputs):
    - orbit/specs/<date>-legacy-classifier-cleanup/  (internal refactor;
      retires --model-type and the Sense/Tiered/Flat dead branches in
      crates/finetype-model — supports card 0006's "Maintainer
      affordances stay hidden" scenario)
    - orbit/specs/<date>-release-polish-v0619/  (docs refresh + repo
      hygiene + drift-check CI gate per memos
      2026-04-27-stale-documentation, repo-cleanliness,
      sweep-script-graveyard)
  These ship in the v0.6.19 release PR alongside the three rally cards
  but are not orchestrated by the rally itself — they're release
  engineering, not capability deliverables.
