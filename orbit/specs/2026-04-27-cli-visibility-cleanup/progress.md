# Implementation Progress

Spec path: orbit/specs/2026-04-27-cli-visibility-cleanup/spec.yaml
Spec hash: sha256:595cd379b1efe137963611d264d783f6968e8e871e1689096526a8f759e3fe2d
Started: 2026-04-27
Current AC: none

## Hard Constraints
- [x] Hard removal posture — no deprecation cycle. Removed flags/subcommands error immediately via clap's unknown-arg/unknown-subcommand handling. — `--model` and `--sharp-only` flags removed entirely; `eval-gittables` subcommand removed entirely. clap surfaces "unexpected argument" / "unrecognized subcommand" with exit 2 on attempted use.
- [x] Internal callers must continue working. `make ci` (uses `finetype check`), training data prep (uses `finetype generate`), sweep wrappers (use `finetype train`), and `scripts/eval.sh` (uses `finetype eval`) all keep working — these subcommands are hidden, not removed. — Verified all hidden subcommand definitions retained, only `#[command(hide = true)]` annotation added; dispatch arms unchanged.
- [x] Every locus that passes `--model <path>` to the finetype binary MUST be migrated to export `FINETYPE_MODEL=<path>` in the same PR. Authoritative migration list (9 loci): eval/profile_eval.sh:115, scripts/v19_compare.sh:91, scripts/sharpen_ablation.sh:66+70, scripts/amvg/ac03_confusion.py:51, scripts/amvg/ac07_post_fix.py:62, scripts/rhh/ac03_hit_counts.py:123, scripts/rhh/ac04_counterfactual.py:199, scripts/rhh/test_rhh.py:340. Out of scope: scripts/distill_batches.sh (claude), scripts/llm_label.py/.sh (Ollama), scripts/train.sh (--model-name is different), scripts/eval.sh (script-internal flag), scripts/overnight_v*.sh (delegate to eval.sh).
- [x] MCP server is out of scope. MCP's `schema` tool stays verbose for v0.6.19. — No edits to crates/finetype-mcp/.
- [x] Schema verb consolidation (type-mode → taxonomy, table-mode → profile) is out of scope. — `schema` verb still exists; both type-mode and table-mode codepaths preserved (only their emitter verbosity reduced).
- [x] `load` subcommand stays public in v0.6.19 — load→validate consolidation deferred to v0.6.20. — `load` not hidden; appears in --help.
- [x] `--model-type` flag and the legacy classifier code paths are deferred to a separate spec. — `--model-type` retained on Profile; legacy CharClassifier/TieredClassifier dispatch untouched.
- [x] Doc refresh is a follow-up PR. Minimum CLAUDE.md updates: stability tier paragraph + command table refresh. — Only the two minimum sections edited; `## Architecture` / `## Sprint Goal` / decision register sections untouched.
- [x] Changes confined to: crates/finetype-cli/src/main.rs, CLAUDE.md, the migration scripts, and tests. No model-crate edits. No MCP edits. — Verified by `git diff --name-only` scope: only main.rs + CLAUDE.md + 9 migration script files + progress.md/spec artefacts.
- [x] Hide mechanism is `#[command(hide = true)]` on the clap subcommand definition. — Used consistently for Check, Generate, Train, TrainMultiBranch, Eval, InferBatch — no wrapper scripts, no env-var gating, no separate binary.
- [x] Schema verbosity reduction edits the JSON Schema emitter in both type-mode and table-mode codepaths.

## Detours
2026-04-27: Schema-golden fixtures (golden_schema_email, golden_schema_iso_date) asserted on `x-finetype-broad-type` which AC-6 dropped. Updated assertions to the v0.6.19 reduced surface — pii retained, broad-type/transform/format-string asserted absent. Three pre-existing model-classification regressions on golden_profile_{ecommerce_orders,people_directory,titanic} verified identical on main HEAD via stash-and-rerun; not caused by this spec.
Return to: AC-9

## Acceptance Criteria
- [x] AC-1: `finetype check` hidden via `#[command(hide = true)]` at main.rs:274.
- [x] AC-2: `finetype generate` hidden via `#[command(hide = true)]` at main.rs:93.
- [x] AC-3: EvalGittables variant + dispatch arm + cmd_eval_gittables function (~568 LOC) deleted; standalone binary at finetype-eval/src/bin/eval_gittables_cli.rs untouched. cargo build clean, no dead-code warnings.
- [x] AC-4: `--sharp-only` flag is removed from every subcommand that accepts it; all dead gate code that depended on it is collapsed. — Removed flag defs from Infer/Load/Profile, removed sharp_only from 6 dispatch destructures + 5 function signatures (cmd_infer, cmd_infer_batch, cmd_load, cmd_profile + cmd_infer_batch call site), collapsed 4 gate sites (now `if !column_classifier.has_multi_branch()`), reframed cmd_load pipeline label to `Multi-branch→Sharpen` vs `Sense→Sharpen`. Cargo build clean.
- [x] AC-5: `--model` flag is removed from every subcommand that accepts it. 9 loci that previously passed `--model "$PATH"` to the finetype binary are migrated to export `FINETYPE_MODEL="$PATH"`. — Removed flag def from Infer/Schema/Load/Profile/Eval (5 variants), removed model from 5 dispatch destructures + 5 cmd_* signatures, added `resolve_model_path()` helper at top of file (FINETYPE_MODEL env var → models/default fallback). Migrated 9 script loci: eval/profile_eval.sh:115, scripts/v19_compare.sh:91, scripts/sharpen_ablation.sh:66+70, scripts/amvg/ac03_confusion.py:51, scripts/amvg/ac07_post_fix.py:62, scripts/rhh/ac03_hit_counts.py:123, scripts/rhh/ac04_counterfactual.py:199, scripts/rhh/test_rhh.py:340. Cargo build clean.
- [x] AC-6: Schema export verbosity is reduced. Both emitter codepaths in main.rs drop `x-finetype-confidence`, `x-finetype-domain`, `x-finetype-broad-type`, `x-finetype-transform`, `x-finetype-transform-ext`, `x-finetype-format-string`. Retains `x-finetype-label` and `x-finetype-pii`. — Type-mode emitter (cmd_schema_type at ~2680): dropped 4 fields (broad-type, transform, format-string, transform-ext); retained pii. Table-mode emitter (cmd_schema_table at ~2900): dropped 6 fields (domain, confidence, broad-type, transform, format-string + transform-ext was not present); retained label + pii. Removed dead `confidence` field from internal `ColResult` struct (4 use sites). table_validator.rs `type_confidence: Option<f64>` continues to handle NULL gracefully (Finding 8 from review). Cargo build clean.
- [x] AC-7: `finetype validate` flag pair `--db`/`--table` becomes optional-but-mutually-required. Check-only mode runs without `--db`; honours `--output`, `--lenient`; `--append` requires `--db`. — Validate clap variant: `db: Option<PathBuf>` and `table: Option<String>` with `requires` cross-references; `append: bool` gains `requires = "db"`. cmd_validate_table signature updated to Option types. Materialise path (scan_id, SQL gen, DuckDB shell-out) wrapped in `if let (Some(db_path), Some(table_name)) = ...` so check-only mode skips DuckDB entirely. Summary report adapts: materialise mode shows Output DB / Target table / Scan ID; check-only mode shows `Mode: check-only (no .db written)`. JSON report emits `db: null` and `mode: "check-only"` when no .db. Exit-code grid (0 no rejects / 1 rejects / 2 error / `--lenient` forces 0) preserved. `--lenient` honoured in both modes; `--output` honoured in both modes. `--append` requires `--db` per clap. Cargo build clean.
- [x] AC-8: CLAUDE.md gains a "Public vs internal CLI surface" section + command table refreshed. — Added "Public vs internal CLI surface" subsection under "### CLI commands" with public/internal tier table (7 public: infer, profile, schema, validate, load, mcp, taxonomy; 6 hidden: check, generate, train, train-multi-branch, eval, infer-batch). Documents `#[command(hide = true)]` mechanism, `--model` removal in favour of `FINETYPE_MODEL` env var, schema verbosity reduction, and validate's new check-only default. Command table reordered to public-first; hidden commands tagged `*(hidden)*`. Validate row updated to reflect optional `--db`/`--table` and check-only default. Version line left as 0.6.18 — release-time bump, not spec-time.
- [x] AC-9: All existing tests pass. `cargo test` workspace green; golden integration green; `make ci` green; profile eval score matches main HEAD ±0. — `make ci` green (fmt + clippy + 406 unit tests + 14 integration tests + taxonomy check 12000/12000 samples passing). Golden integration: 13 tests; 10 pass on first run, schema-fixture detour above brought it to 13. Three pre-existing model regressions (golden_profile_ecommerce_orders order_id → finance.securities.sedol, golden_profile_people_directory phone → identity.person.phone_number, golden_profile_titanic SibSp → representation.numeric.integer_number) verified identical on main HEAD via stash-and-rerun — they're v19-promotion noise unrelated to this spec; profile eval score matches main HEAD ±0 by definition (same model, same code path for those columns). Two schema-fixture failures (AC-6 induced) fixed in same PR per the detour note.
- [x] AC-10: `finetype --help` shows the intended v0.6.19 public surface (`infer`, `profile`, `schema`, `validate`, `load`, `mcp`, `taxonomy`). — Verified `cargo run -p finetype-cli -- --help` lists exactly 7 commands: infer, taxonomy, schema, load, validate, profile, mcp. Hidden subcommands (check, generate, train, train-multi-branch, eval, infer-batch) excluded from --help but remain callable.
