# Pre-commit hooks for finetype.
# Install: `pre-commit install`
# Run:     `pre-commit run --all-files`

repos:
  - repo: local
    hooks:
      - id: orbit-verify
        name: orbit-state round-trip + index rebuild
        entry: orbit verify
        language: system
        pass_filenames: false
        always_run: true
