# Implementation Progress

Spec path: .orbit/specs/2026-04-24-remove-header-hints/spec.yaml
Spec hash: sha256:06708cd44018846bed24cce2e0c8f2ec8b019c00c870586e384618115ccf872a
Started: 2026-04-24
Current AC: none

## Hard Constraints
- [x] No source-code removal in this spec. column.rs / semantic.rs / main.rs stay intact. Deliverable is measurement + roadmap, not a behaviour change.
- [x] Model2Vec semantic-hint layer (semantic.rs) out of scope. Threshold gates (cross_domain 0.85, same_category 0.95) stay.
- [x] Granularity is rule-family, not per-arm. ac-01 is source of truth for the canonical family list.
- [x] Measurement against the full 448-row eval manifest using sherlock-v16 (models/default).
- [x] Model-covered ≥80% threshold; model-gap <80%. Named, not debated.
- [x] Per-family disablement via single cfg-gated hook per function entry point (not per-arm). `rhh-instrumentation` feature flag. Off-feature compiles to zero overhead.
- [x] All diagnostics under diagnostics/ with reproducible regeneration scripts under scripts/rhh/. Every TSV has a schema comment header.
- [x] Test prefix `rhh`. Tests named rhh_acNN_*.

## Detours

## Acceptance Criteria
- [x] ac-01: Family inventory TSV (diagnostics/rhh_family_inventory.tsv) enumerating every rule-based header-hint family with file/line/domain/type_targets. — 22 families written (1 match_table, 6 substring, 12 branch, 3 legacy_sense); all 4 rule_family_class values present. Schema extended with `keep_required` column flagging the 2 threshold-gate families (`header_hint_cross_domain`, `header_hint_same_category`) that spec constraint #2 forbids removing.
- [x] ac-02: Rust instrumentation — `rhh-instrumentation` feature + env var RHH_DISABLE_HINTS hook at each family's function entry point. — Feature added to finetype-model/Cargo.toml; `rhh` module with `is_disabled(family_id)` hook; 17 disable flags instrumented across `apply_header_sharpen` (10) and `header_hint` (7); 5 default-build invariant tests + 1 consolidated on-feature scenario test (5 sub-scenarios). Default build: zero-cost (feature-gated const fn path). 415 on-feature tests pass, full default suite green.
- [x] ac-03: Per-family hit counts on full 448-row eval (diagnostics/rhh_hit_counts.tsv). — 36 files profiled, 453 columns, 142 with disambiguation_rule. Direct RHH hits: header_hint_cross_domain 40 (keep_required), header_hint_hardcoded 11, header_hint_generic 11, header_hint_same_category 3 (keep_required), header_hint_geo_override 2, header_hint 1, header_hint_sci_measurement 1 = 69 total. 7 internal families (header_hint_table + 6 substring_matcher_*) fire inside header_hint() without a dedicated rule prefix — measured via ac-04 counterfactual. sense_* legacy families all zero (not on default multi-branch path).
- [x] ac-04: Per-family counterfactual predictions with RHH_DISABLE_HINTS set (diagnostics/rhh_counterfactual.tsv) with pinned model path + weights sha256. — 9856 rows (22 families × 448 GT-covered columns). Model pinned: models/default → sherlock-v16, weights sha256: 58dcba8ea723f4f4d4091d1ef348ad3b19f7700396bd47ecee759409a28f9ecc. Scoring uses schema_mapping.csv-backed label equivalence (mirrors eval_profile.sql). Summary sidecar at rhh_counterfactual_summary.tsv. Largest impact: header_hint_table 31 changes / -24 correct. 10 branch families have 0 label changes under disablement. 3 legacy_sense families have 0 changes (not on default multi-branch path).
- [x] ac-05: Family classification (model-covered / model-gap / no-hit) per 80% threshold (diagnostics/rhh_classification.tsv). — 22 families classified: 2 model-covered (substring_matcher_datetime 1.000, header_hint 1.000), 10 model-gap (incl. both keep_required threshold-gates: cross_domain 0.333, same_category 0.667), 10 no-hit (incl. 3 legacy_sense). Hit definition: label_changed==1 in counterfactual (uniform for direct and internal measurement modes). 4 rhh_ac05_* tests added (boundary ×3 + arithmetic ×1) in scripts/rhh/test_rhh.py — all pass.
- [x] ac-06: Domain rollup (diagnostics/rhh_domain_rollup.tsv). — 8 rows (7 taxonomy domains + cross-domain). Only READY domain: datetime (1 family, all model-covered). Container NO_EVIDENCE (zero families, expected). Remaining 5 taxonomy domains + cross-domain all NEEDS_FORTIFICATION. Total across rows = 22 (matches inventory).
- [x] ac-07: Roadmap synthesis (diagnostics/rhh_roadmap.tsv) with proposed_stage and blocked_on. — 22 rows joining ac-01 inventory + ac-05 classification. Tallies: 12 removable (blocked_on empty — no-hit + model-covered), 9 training-data-fortification-<type> (model-gap non-amount), 1 v19-retrain (header_hint_table targets finance.currency.amount_* variants; MADR 0065 gate). proposed_stage distribution: cross-domain-stage 8, geography 6, representation 3, identity 2, datetime 1, finance 1, technology 1. v19-retrain gate widened from spec's literal `domain==finance` rule to `any("finance.currency.amount_" in type_targets)` so cross-domain header_hint_table (whose amount-variant targets are the actual blocker per MADR 0065) routes correctly; rule documented in script docstring.
- [x] ac-08: Methodology doc (diagnostics/rhh_methodology.md, ≥500 words, 5 required sections). — 1184 words. Sections: Threshold Rationale (80% policy choice + model-at-large alignment), Measurement Approach (counterfactual, label_changed hit definition, schema-mapping-backed equivalence), Instrumentation Design (single cfg-gated hook per function entry, 17 flags across 2 functions, zero-cost default build), Limitations (5 documented: sibling-context not permuted, per-arm deferred, 80% is policy, corpus fixed, model pinned by sha256), Reproducibility Steps (6-step invocation order + test commands + regenerate_all.sh pointer).
- [x] ac-09 (gate): Reproducibility — regenerate_all.sh byte-identical on same machine (with FP drift carve-out for counterfactual/classification). — scripts/rhh/regenerate_all.sh committed + executable; runs ac-01/03/04/05/06/07 in order with --fast skip for ac-04. Pre/post sha256 fingerprint diff writes diagnostics/rhh_drift_log.txt on divergence and exits 1. diagnostics/rhh_fingerprints.sha256 committed (7 artefacts hashed). Smoke-tested byte-identical re-run across the 4 pure-deterministic scripts (ac-01, ac-05, ac-06, ac-07) AND ac-03 (profile-pipeline, pinned model). Latent bug surfaced: original ac-01 substring_matcher_* locators no longer matched column.rs after ac-02 instrumentation added !disable_* guards; fixed by re-bracketing with !disable_<domain> regex (now lock-step with ac-02 markers). rhh_ac09_reproducibility_* tests added (script-present-and-executable + fingerprints-match) — passing.
- [x] ac-10 (gate): Publication — all TSVs committed, PR description has 4 required sections. — PR description drafted at .orbit/specs/2026-04-24-remove-header-hints/pr-description.md containing all 4 required sections: (a) domain rollup summary table, (b) proposed per-domain stage sequence (datetime → technology → identity → representation → geography → cross-domain → finance), (c) explicit model-gap families table with blocked_on values (10 families, 1 v19-retrain + 9 training-data-fortification-*), (d) pointer to rhh_methodology.md. All 9 diagnostics/rhh_*.tsv/md/sha256 artefacts staged (untracked — ready for commit). All rhh instrumentation files staged. Actual staging + PR submission performed at drive-completion; forked /orb:review-pr will verify the 4 sections against this draft.
- [x] ac-11: MADR 0042 execution log appended. — `## Execution Log` section appended to .orbit/choices/0042-remove-regex-header-hints.md. References this spec path, rhh_roadmap.tsv as the execution artefact, companion TSVs, pinned model sha256, and per-domain successor trajectory. Decision's `status` field unchanged (stays `accepted`) per ac-11 contract.
