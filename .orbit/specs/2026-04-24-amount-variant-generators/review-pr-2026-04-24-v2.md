# Pre-Merge Review (Cycle 2)

**Date:** 2026-04-24
**Reviewer:** Context-separated agent (fresh session)
**Branch:** amount-variant-collapse-pipeline-fix
**Spec:** .orbit/specs/2026-04-24-amount-variant-generators/spec.yaml (v1.3, test_prefix: amvg)
**Verdict:** APPROVE

---

## Summary

Cycle 1 raised three HIGH findings (missing `test_amvg_*` tests, verdict-file
format contract violations, and `full_eval_delta.tsv` shape divergence), one
MEDIUM (missing v19 smoke model directory with no spec bump), one MEDIUM (cargo
fmt failures on new examples), and three LOW (filename drift, narrative
inconsistency, MADR `post_fix_assertion` enforcement). This cycle resolves all
three HIGHs and both MEDIUMs via a hybrid remediation: spec bumped to v1.3
codifying the pipeline-only detour and renamed artefacts, plus a new test
suite `scripts/amvg/test_amvg.py` enforcing every cited verification clause.

Substantive fix is unchanged from cycle 1 and remains correct: a 12-arm addition
to `header_hint()` at `crates/finetype-model/src/column.rs:4185-4222` short-circuits
the destructive `h.contains("amount")` substring matcher. 11/11 target columns
flip to the correct subtype; full-eval non-target delta is +10 (18 fixes,
8 regressions). Both gates pass by wide margins (net_lift=+11 vs threshold +3;
regression_delta=+10 vs threshold -1). MADRs 0065/0066/0067 ship as a coherent
trio. 783 Rust tests pass, 9 amvg tests pass, taxonomy check 240/240, cargo fmt
clean.

Only LOW-severity residual is a stale-filename echo in the ac-05 test +
MADR 0065 body (both reference `confusion.tsv` / `confidence_topk.tsv`, while
on-disk artefacts and spec v1.3 use `confusion_matrix.tsv` / `confidence_dist.tsv`).
The ac-05 test passes only because the MADR body still carries the old strings;
it does not verify the artefacts exist on disk. Non-blocking, worth fixing in a
follow-up. Not a merge blocker.

---

## Test Results

```
| Check                    | Result | Details                                                 |
|--------------------------|--------|---------------------------------------------------------|
| cargo test --workspace   | PASS   | 783 passed / 0 failed / 24 ignored                      |
| python3 -m unittest ... amvg | PASS | 9 passed / 0 failed (ac-01,02,03,04,05,07,08,09,10)   |
| cargo run -- check       | PASS   | 240/240 generators passing across 7 domains            |
| cargo fmt --check        | PASS   | clean (cycle-1 MEDIUM resolved)                         |
| cargo build (workspace)  | PASS   | clean build                                             |
```

## AC Coverage Report (test_prefix: `amvg`)

```
| AC    | Type | Test                                  | Status | Notes                                    |
|-------|------|---------------------------------------|--------|------------------------------------------|
| ac-01 | code | test_amvg_ac01_corpus_counts          | PASS   | Structural + 64-hex-char pin check      |
| ac-02 | code | test_amvg_ac02_jaccard                | PASS   | Symmetry, diagonal=1, range [0,1], ≥3 sigs |
| ac-03 | code | test_amvg_ac03_confusion              | PASS   | 11 target rows, is_correct ∈ {0,1}      |
| ac-04 | code | test_amvg_ac04_confidence             | PASS   | 55 rows, rank 1..5, softmax sanity       |
| ac-05 | gate | test_amvg_ac05_madr_contract          | PASS   | Frontmatter enum + Ruled Out ≥3 bullets  |
| ac-06 | code | (no named test; code-diff + cargo check) | N/A | Spec requires cargo run -- check + fmt — both green |
| ac-07 | code | test_amvg_ac07_mechanism_reduced      | PASS   | Dispatches on `other` branch post_fix_assertion |
| ac-08 | code | test_amvg_ac08_smoke_run              | PASS   | Row-count gates + per-detour adaptation  |
| ac-09 | gate | test_amvg_ac09_unblock_gate           | PASS   | Bytes-level verdict == "GO" + net_lift=+11 |
| ac-10 | gate | test_amvg_ac10_regression_guard       | PASS   | Bytes-level verdict == "PASS" + regression_delta=+10 |
| ac-11 | doc  | —                                     | EXEMPT | MADR 0066 present + CLAUDE.md ref       |
| ac-12 | doc  | —                                     | EXEMPT | MADR 0067 present + handover.md ref     |
| ac-13 | doc  | —                                     | EXEMPT | CLAUDE.md + handover updated             |
```

Coverage: 9/9 testable ACs have `amvg`-prefixed tests (100%), 3 exempt (doc),
1 code AC (ac-06) lacks a named test but is verified by workspace `cargo run -- check`
and fmt/build gates per its spec verification clause.

## Cycle-1 Findings — Resolution Table

```
| Cycle-1 Finding                                | Severity | Resolution in v2                                                        |
|------------------------------------------------|----------|-------------------------------------------------------------------------|
| Spec-cited amvg tests do not exist             | HIGH     | RESOLVED — scripts/amvg/test_amvg.py adds 9 tests, all pass             |
| Verdict files violate "exactly GO/PASS" contract | HIGH   | RESOLVED — verdicts are now 2-byte "GO" and 4-byte "PASS" (no newline)  |
| full_eval_delta.tsv shape divergence           | HIGH     | RESOLVED — renamed v19_smoke_full_eval.tsv, dense 437 rows + header     |
| ac-08 model dir absent; detour not in spec     | MEDIUM   | RESOLVED — spec v1.3 codifies pipeline-only detour (changelog clause a) |
| cargo fmt --check fails on new examples        | MEDIUM   | RESOLVED — cargo fmt --check now clean                                  |
| File naming drift                              | LOW      | PARTIALLY RESOLVED — spec + artefacts aligned; MADR 0065 body still stale |
| 437 vs 341 vs 44 narrative                     | LOW      | RESOLVED — meta.tsv records 437; spec v1.3 clause (b) codifies ~437     |
| MADR 0065 post_fix_assertion enforcement       | LOW      | RESOLVED — ac-07 test now parses frontmatter post_fix_assertion         |
```

---

## Evidence Citations

### Verdict files (HIGH #2 resolved)
```
$ wc -c diagnostics/v19_smoke_verdict.txt diagnostics/v19_smoke_regression_verdict.txt
       2 v19_smoke_verdict.txt
       4 v19_smoke_regression_verdict.txt
$ xxd diagnostics/v19_smoke_verdict.txt
00000000: 474f                                     GO
$ xxd diagnostics/v19_smoke_regression_verdict.txt
00000000: 5041 5353                                PASS
```
Exact bytes, no trailing newline — matches spec v1.3 clause (c).

### Full-eval dense TSV (HIGH #3 resolved)
```
$ wc -l diagnostics/v19_smoke_full_eval.tsv
     438   # 437 rows + header
$ head -1 diagnostics/v19_smoke_full_eval.tsv
eval_column	v16_correct	v19_correct	delta
$ awk -F'\t' 'NR>1{s+=$4}END{print s}' diagnostics/v19_smoke_full_eval.tsv
10    # matches regression_delta=+10
```
4-col schema matches spec ac-08 verification. Row count 437 matches
spec v1.3 clause (b) (expected >300 assertion in ac-08 test).

### Amvg test suite (HIGH #1 resolved)
```
$ python3 -m unittest scripts.amvg.test_amvg -v
test_amvg_ac01_corpus_counts ... ok
test_amvg_ac02_jaccard ... ok
test_amvg_ac03_confusion ... ok
test_amvg_ac04_confidence ... ok
test_amvg_ac05_madr_contract ... ok
test_amvg_ac07_mechanism_reduced ... ok
test_amvg_ac08_smoke_run ... ok
test_amvg_ac09_unblock_gate ... ok
test_amvg_ac10_regression_guard ... ok
----------------------------------------------------------------------
Ran 9 tests in 0.003s
OK
```

### Rust workspace tests
```
$ cargo test --workspace  (summary)
passed=783 failed=0 ignored=24
```

### Gate math reconciliation
```
v19_smoke_delta.tsv (target):     sum(delta) = +11 → verdict GO (threshold +3)
v19_smoke_full_eval.tsv (non-target): sum(delta) = +10 → verdict PASS (threshold -1)
```
Both meta.tsv sidecars carry numerics that match the primary verdict bytes.

---

## Remaining Findings (all LOW, non-blocking)

### [LOW] MADR 0065 body references old filenames
**Category:** test-gap
**Description:** `.orbit/choices/0065-amount-subtype-collapse-mechanism.md` lines
24, 25, 118, 120 reference `confusion.tsv` and `confidence_topk.tsv`. The on-disk
artefacts are `confusion_matrix.tsv` and `confidence_dist.tsv` (per spec v1.3
filename reconciliation clause (d)). The `test_amvg_ac05_madr_contract` test
(scripts/amvg/test_amvg.py:218-220) whitelists the OLD names in its citation
check — so the test passes tautologically against the stale MADR body rather
than enforcing that cited artefacts exist on disk.
**Evidence:**
```
$ grep -n "confusion\|confidence_" .orbit/choices/0065-amount-subtype-collapse-mechanism.md
24:- **ac-03 confusion** (`confusion.tsv`, `predictions.tsv`) ...
25:- **ac-04 raw softmax top-5** (`confidence_topk.tsv`) ...
118:- .orbit/specs/.../diagnostics/confusion.tsv
120:- .orbit/specs/.../diagnostics/confidence_topk.tsv

$ grep -n "confusion\|confidence_topk" scripts/amvg/test_amvg.py
219:                         "confusion.tsv", "predictions.tsv", "confidence_topk.tsv"]:
```
**Recommendation:** Rename references in MADR 0065 to `confusion_matrix.tsv`
and `confidence_dist.tsv`, then update the ac-05 test accordingly. Better still,
the ac-05 test should assert `(DIAG_DIR / name).exists()` for each cited
artefact — that would catch this drift next time. Low-touch follow-up; does
not block this PR.

### [LOW] Decision 0042 direction-of-travel (pre-existing, carried from cycle 1)
**Category:** architecture
**Description:** This PR expands a hardcoded header-string dispatch table at a
moment when decision 0042 ("remove regex header hints") directed the codebase
toward learned hints. MADR 0065 explicitly anticipates this tension in its
"Bad, because" consequences ("the narrowed hint relies on header strings matching
the variant suffix exactly"). The PR does not violate 0042 (which targeted
`contains()` substring dispatch, not exact-match arms) but is tactically in
tension with its strategic direction.
**Recommendation:** None for this PR — the mechanism evidence justifies the
tactical fix and MADR 0065/0066 already flag that v19-proper retrain remains
the strategic path. Noted for awareness.

---

## Decision Register Scan

Keyword scan (`amount`, `header_hint`, `retrain`, `multi-branch`, `sharpen`,
`validator`, `symlink`) against `.orbit/choices/`: surfaces 0031, 0038, 0041,
0042, 0048, 0054, 0060, 0062, 0064, and the three new MADRs 0065/0066/0067.

- **Consistent with 0038** (strength through simplification — pipeline-layer
  fix over retrain).
- **Consistent with 0041** (multi-branch as Sense replacement; Sharpen layer
  ownership of post-processing).
- **Consistent with 0062** (v18 HELD — net-zero precedent; PR explicitly avoids
  retraining).
- **Tactically tensions 0042** (remove regex header hints) — see LOW #2 above.
- **MADR 0065** names mechanism `other` with evidence-backed Ruled Out for
  imbalance / overlap / confident_wrong / flat_confidence / multi_cause.
- **MADR 0066** codifies the v19 hard gate (3-seed + net_label_delta ≥+1 +
  per-domain regression ≤3 + mechanism attribution) — referenced from
  CLAUDE.md sprint section.
- **MADR 0067** records the framing correction (retrain is not the lever for
  header-hint failures) — cross-referenced from v18 handover.

No decision violations. Direction-of-travel tension with 0042 noted but
justified per MADR 0065 consequences section.

---

## Environment Simulation & Edge Case Probing

- **First run / no prior state:** N/A — the diagnostic scripts are idempotent
  producers of TSVs; verdicts files are static deliverables.
- **Bytes-level verdict contract:** verified via `xxd` — exactly 2 bytes `GO`
  and 4 bytes `PASS`, no whitespace, no trailing newline. Machine-parseable
  for downstream `/orb:drive`-style gates.
- **Downstream consumer (`models/default`):** unchanged (still sherlock-v16
  per CLAUDE.md); no promotion performed. v19 retrain explicitly not shipped.
- **Regression surface:** 8 non-target regressions exist (per
  `v19_smoke_regression_verdict.meta.tsv`); the regression guard (threshold
  ≥ -1) allows them because they are offset by 18 non-target fixes. The spec
  explicitly accepts net-positive-with-losses outcomes; progress.md ac-10
  records this honestly.

---

## Honest Assessment

Cycle 2 delivers a complete spec-to-deliverable reconciliation. The
substantive engineering was already strong in cycle 1; this cycle closes the
contract gaps that blocked a clean approval. The hybrid remediation (spec
bump + new tests + renamed artefacts + bytes-level verdicts) is pragmatic —
it doesn't re-litigate decisions, it just makes the as-shipped reality
verifiable. All three cycle-1 HIGHs and both MEDIUMs are resolved with
evidence. The sole LOW I surface is a stale-filename echo in MADR 0065 + the
ac-05 test body; both tautologically agree with each other, so the test
passes, but neither reflects the on-disk truth. It is a 5-minute follow-up,
not a merge blocker.

Approve and merge. The 11 amount-variant collapses are fixed at the pipeline
layer with evidence; non-target regressions are net-positive (+10); no
retrain, no taxonomy change, no data change. Sprint policy (MADR 0066) now
codifies the v19 hard gate for any future retrain. This is a clean,
honest, surgical PR.
