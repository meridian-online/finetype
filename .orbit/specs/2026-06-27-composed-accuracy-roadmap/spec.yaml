id: 2026-06-27-composed-accuracy-roadmap
goal: Raise composed gold accuracy from 0.812 toward the audited ~0.94 ceiling via eval cleanup, swap-proof Tier-A Sharpen rules, kill-switched Tier-B residual-precedence, deferred header-corroboration pilots, and the free batch-path latency win — sequenced so next session's Sense work starts from a clean baseline.
cards:
- '0019'
status: open
labels:
- sharpen
- eval
- latency
- composed-accuracy
acceptance_criteria:
- id: ac-1
  description: |
    Live re-score on 0.6.37 locks the true composed baseline (audit measured
    753/927=0.812; ~8 phantom rows idx 27,29,63-67,78 already correct in live ->
    expect ~0.821). The errors_enriched.tsv snapshot is stale; re-score first.
  gate: false
  checked: false
  ac_type: observation
- id: ac-2
  description: |
    Eval cleanup applied: 14 clean gold re-adjudications (engine already correct)
    + 31 values-missing source columns re-pulled + mixed-panel run on the 14
    defensible contested rows. Curation, not an accuracy gain. (BACKLOG #28/#29.)
  gate: false
  checked: false
  ac_type: config
- id: ac-3
  description: |
    Tier-A swap-proof value rules shipped (schema_fail_demotion scope, NPI/ISBN
    checksums, URL/utc/numeric-fallback recovery) — demote/decline-only, each
    gold-no-regression + corpus-honest GO. Composed -> ~0.86-0.88. (BACKLOG #1-#11.)
  gate: false
  checked: false
- id: ac-4
  description: |
    Tier-B residual-precedence shipped per choice 0096 (increment->integer,
    veto_shape_fallback id+epoch arms, binary_vocab full-column) with RHH kill
    switches + BLOCKING corpus-honest. Flagged co-adapted: re-score after the
    next-session Sense swap. Composed -> ~0.88. (BACKLOG #12-#16.)
  gate: false
  checked: false
- id: ac-5
  description: |
    Tier-C header-corroboration pilots — geography cluster SPLIT, IATA/TLD
    closed-sets (IANA TLD + IATA lists added to repo), each individually through
    the blocking corpus-honest gate (expect ~1-in-4 NO-GO). Composed -> ~0.90.
    (BACKLOG #17-#27.)
  gate: false
  checked: false
  ac_type: observation
- id: ac-6
  description: |
    Free batch-path latency plumbing landed: load_taxonomy+compile_validators
    hoisted out of the per-file loop (profile.rs:243,257); N files per duckdb
    spawn. Accuracy-identical; batch per-file marginal ~halved. (Card 0006.)
  gate: false
  checked: false
- id: ac-7
  description: |
    Next-session Sense seam documented: two-stage/abstaining head (measure
    composed pass-through) + free latency wins (single-4M revert, deterministic
    fast-path); re-fit the Tier-B co-adapted vetoes against the new model.
  gate: false
  checked: false
  ac_type: doc
