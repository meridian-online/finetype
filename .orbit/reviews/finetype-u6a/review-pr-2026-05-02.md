**Verdict:** APPROVE

# Pre-Merge Review

**Date:** 2026-05-02
**Reviewer:** Context-separated agent (fresh session)
**Branch:** drive/finetype-u6a-agent-ready-skills
**Bead:** finetype-u6a

---

## Test Results

| Check | Result | Details |
|-------|--------|---------|
| Cargo unit tests | PASS | 597 passed, 2 ignored across 3 suites (1.41s) |
| Smoke tests (`tests/smoke.sh`) | PASS | 24/24 passed |
| Drift script — happy path | PASS | `verify-cli-skill-coverage.sh` exits 0; reports "aligned with finetype 0.6.19 across 7 subcommands" |
| Drift script — missing binary | PASS | `FINETYPE_BIN=nonexistent_binary` → exit 2 with helpful message |
| Drift script — missing SKILL.md | PASS | `SKILL_PATH=/nonexistent/path` → exit 2 with helpful message |
| Drift script — idempotency | PASS | Two consecutive runs both exit 0 |
| Drift script — drift detection | PASS | Injecting `--bogus-flag` into a temp SKILL.md copy yields exit 1 with `OBSOLETE FLAG` line |
| AC coverage (test files) | 0/4 by name | All four ACs are documentation/inspection ACs (see honest assessment) |

## AC Coverage Report

| AC | Status | Verification |
|----|--------|--------------|
| ac-01 | PASS | Pipeline-skill walkthrough verified by inspection of `.claude/skills/finetype-pipeline/SKILL.md` (steps 1–4 cover `profile` → `profile -o json-schema` → `validate --db --table`) and the new `.claude/skills/finetype-pipeline/CHECKLIST.md` (74 lines, three positive must-run steps + four negative checks); zero references to a `load` verb in either file. |
| ac-02 | PASS | Banner reads `FineType v0.6.19`; per-subcommand flag inventory reconciled against live `finetype <subcmd> --help` for all 5 visible subcommands (`profile`, `validate`, `infer`, `taxonomy`, `mcp`) and the 2 hidden subcommands (`generate`, `check`). All cycle-2 divergences closed: `validate --db/--table/--append/--lenient` documented; `--enum-threshold` default 32; `--model-type` default `multi-branch`; `--sharp-only` removed; `profile --stats` documented. Drift script run on this exact tree returns OK. |
| ac-03 | PASS | `scripts/verify-cli-skill-coverage.sh` exists (166 lines, executable); enumerates visible subcommands from `finetype --help` plus a curated `HIDDEN_SUBCOMMANDS=(generate check)` allowlist; uses `awk` to slice SKILL.md by `### \`finetype <cmd>\`` headers and `comm` to diff sets; exits 0 on alignment, 1 on drift, 2 on script error. All four exit paths exercised above. |
| ac-04 | PASS | Neither SKILL.md instructs the user to run `claude mcp add …`, define `ANTHROPIC_API_KEY`, or set environment variables. The only `mcp` references are to FineType's own `finetype mcp` subcommand — explicitly carved out as a non-violation by the AC. CHECKLIST.md is similarly clean. |

## Findings

No CRITICAL or MAJOR findings. The implementation matches every clause of every AC verbatim against evidence runnable in this session.

### [INFO] Drift script is bash-3.2 portable on macOS
**Category:** quality
**Description:** The script avoids `mapfile` (not present in macOS's bash 3.2) and uses a `while read … done < <(…)` loop with a `${visible[@]+…}` guard for `set -u` safety. This is the right call for a developer-machine artefact and worth noting because it survived the obvious portability trap.
**Evidence:** `scripts/verify-cli-skill-coverage.sh:106–116`
**Recommendation:** None — keep as is.

### [INFO] Hidden-subcommand allowlist is the right shape for a follow-up CI gate
**Category:** future-work
**Description:** The `HIDDEN_SUBCOMMANDS=(generate check)` allowlist is the single source of truth for verbs that are functional but not in `finetype --help`. When the v0.6.20 work folds the MCP surface to mirror the CLI fold (referenced in SKILL.md line 66), this list is the natural touchpoint to update. The bead description already flags CI integration as a separate follow-up bead, so no action here — this is just a forward-pointing note.
**Evidence:** `scripts/verify-cli-skill-coverage.sh:32`, SKILL.md line 66 ("v0.6.20 audit will mirror the CLI fold").
**Recommendation:** None for this PR.

### [INFO] CHECKLIST.md positions itself as the pipeline skill's drift artefact
**Category:** quality
**Description:** The checklist's "Maintenance" footer (`CHECKLIST.md:70–74`) explicitly pairs itself with `scripts/verify-cli-skill-coverage.sh` as the corresponding artefact for the pipeline skill's behavioural claim. Symmetric framing — the two skills get parallel verification artefacts — and addresses the bead-description point about making AC-04's "no setup" claim "explicitly testable by inspection."
**Evidence:** `.claude/skills/finetype-pipeline/CHECKLIST.md:70–74`
**Recommendation:** None — keep as is.

---

## Honest Assessment

This is a clean, scope-faithful PR that does exactly what the cycle-3 bead description listed (banner refresh, flag-inventory reconciliation, drift script, pipeline checklist) and nothing more — no scope creep, no parallel-track changes, no MADR churn. All four ACs are verifiable by inspection or by running the drift script, and all four pass when verified that way. The "0/4 ACs have tests by name" coverage line is a non-finding here: ac-01, ac-02, and ac-04 are pure inspection ACs (the documents either say what they claim or they don't, and they do); ac-03 ships an executable harness whose own correctness is verified by exercising its three exit paths (zero/one/two) — which I did, and all three behave as documented. Decisions 0070 and 0071 are the load-bearing MADRs for this work; both are accurately reflected in the new copy on `validate --db/--table` materialisation, the removed `load` verb, and the `profile -o json-schema` / `taxonomy KEY -o json-schema` split. The drift script's hidden-subcommand handling is the subtle bit and it's done right — generate and check are baked into a curated allowlist rather than scraped from `--help`, exactly as the bead description required to avoid silently deleting real documentation. Ready to merge.
