key: next-session-validator-campaign
body: 'VALIDATOR-FIX CAMPAIGN (algo-exists types). ISBN proof-of-concept DONE 2026-06-15 (choice 0098): isbn_checkdigit_veto retired, replaced by ONE generic checksum_substance_guard. The original premise — ''fix the early validator so schema_validation_gate makes the veto redundant'' — was FALSIFIED: that gate is bypassed on the multi-branch default (see schema-validation-gate-bypassed-on-multibranch). The shipped pattern is in [[checksum-substance-guard-pattern]]. NEXT types (each = one-line checksum: YAML directive + a resolve() arm in crates/finetype-core/src/checksum.rs + write the check-digit fn there; NO new Rust veto): numeric & guard-ready now: aba_routing, finance.payment.credit_card_number (luhn), identity.medical.npi, identity.commerce.{ean,upc}, technology.code.imei. Alphanumeric (need a shape-aware guard extension first): finance.securities.{isin,cusip,sedol,lei}, finance.banking.iban. SEQUENTIAL, each gold + corpus-honest gated via scripts/eval_rule.sh --label X --out-dir output/X --default. DO NOT fold checksums into the shared validator (regresses gold via generic value_sharpen rules). Full prioritized list: task t-000073f518b9266878be6a08.'
timestamp: 2026-06-15T06:14:33.847498Z
labels: []
