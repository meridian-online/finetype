key: housekeeping-session-2026-06-15
body: 'Housekeeping/simplification session SHIPPED 2026-06-15 (card 0019 scenario 5), 3 commits on main: Phase 1 (7de4609) clippy FeatureBundle alias + flaky sense_train fix (entity head had no signal) + 6 dead scripts removed; Phase 2 (e2de739) column.rs post-sharpen guard hook (sharpen_and_guard/apply_post_sharpen_guards) + deduped 3 call sites; Phase 3 (4d79e2a) split column.rs 11,974 lines -> column/{mod 2806, tests 6070, value_sharpen, header_sharpen, feature_sharpen, helpers}.rs. KEY TECHNIQUE: the strongest behavior-preserving gate for a refactor is BYTE-IDENTICAL gold predictions (diff predictions_baseline.tsv vs predictions_phaseN.tsv), not just the 0.741 headline — both Phase 2 and 3 verified all 931 identical. Free-fn module split pattern that worked: pub(crate) on moved items + ''pub(crate) use <mod>::*'' re-export from mod.rs + ''use super::*'' in each submodule (except a leaf-helpers module with no type deps). Doc-comment-aware slicing (boundary = next item''s real_start, scan up for leading ///+#[) avoids orphaned docs; cargo fmt -p normalises after. GOTCHA: cargo fmt -p reformats the WHOLE crate, invalidating any pre-recorded line numbers — re-grep item lines after any fmt. Deferred: STATE_CODES 3-way drift (accuracy decision, task t-00007a35), output/ 2.7GB prune (disk-vs-recompute, task t-00007a37), checkpoint-resume (task t-000126f4 item 4).'
timestamp: 2026-06-14T20:27:39.30228Z
labels: []
